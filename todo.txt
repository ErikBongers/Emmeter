* parse classes, IDs and texts properties, regardless of indent, as long as it's deeper than the tag.
  > requires double peek().

* test combination of indent and parenthesis.
  > parenthesis completely reset the indent level.

* test excaped chars ( "\}" and "\"")
  > I think they are not handled yet.
    > they are escaped by the tokenizer but not un-escapced back.

* attribute names can have the format xxx.xxx.xxx.xxx

* attributes can have no value
  : xxx="some value"
  > xxx  --> same as xxx="" !!!

* difference between append and insertAfter isn't clear.